package com.redhat.apps.quarkus.kafka;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeAppResourceIT extends AppResourceTest {

    // Execute the same tests but in native mode.
}